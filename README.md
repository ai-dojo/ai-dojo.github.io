# Trying the website out locally

Start local webserver (http.server module is a built-in in python3, no need to install anything)

```
python3 -m http.server
```

Navigate browser to `http://localhost:8000/`